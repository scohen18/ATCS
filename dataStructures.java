import java.awt.*;
import java.util.*;

class dataStructures{
	public static void main(String [] args)
	{
		Stack testStack = new Stack(); //first in last out
		//only allows access to last element (no indexing)

		Queue testQueue = new Queue(); //first in first out
		//only allows access to first element (no indexing)

		

		// testStack.push("one");
		// testStack.push("two");
		// testStack.push("three");
		//
		// testQueue.push("one");
		// testQueue.push("two");
		// testQueue.push("three");
		//
		// System.out.println("testStack: " + testStack.pop());
		// System.out.println("testStack: " + testStack.pop());


	}
}
